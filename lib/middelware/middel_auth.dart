import 'package:flutter/src/widgets/navigator.dart';
import 'package:get/get.dart';
import 'package:play_store_test/Get_serves/setting_services.dart';

class middleware_log_in extends GetMiddleware {
  // setting_services setting = Get.put(setting_services());

  // @override
  // RouteSettings? redirect(String? route) {
  //   if (setting.curent) {
  //     return RouteSettings(name: "/HomeScreen");
  //   } else {
  //     return RouteSettings(name: "/sign_up");
  //   }
  // }
}
